// 表单枚举

// 页面类型
export enum PageModeEnum {
  Add, // 新增
  Edit // 编辑
}

// 表格大小
export enum TableSizeEnum {
  DEFAULT = 'default',
  SMALL = 'small',
  LARGE = 'large'
}
// 操作类型
export enum OperateType {
  ADD = 'add',
  EDIT = 'edit',
  DELETE = 'delete',
  VIEW = 'view',
  MORE = 'more',
  IMPORT = 'import',
  EXPORT = 'export',
  PRINT = 'print',
  RESET = 'reset',
  SEARCH = 'search',
  CANCEL = 'cancel',
  CONFIRM = 'confirm',
  DOWNLOAD = 'download',
  UPLOAD = 'upload'
}
